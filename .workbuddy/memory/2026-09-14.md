# 2026-09-14 工作记录

- 参考桌面《开发文档.docx》（PocketLedger 全栈项目 20 天学习计划），在当前 React(Vite+TS) 项目（E:\lianzikeji\react learn\tools）实现前端。
- 已实现：路由(react-router-dom)+路由守卫、axios 实例(拦截器携带 Token / 401 自动登出)、zustand 鉴权 store、API 层(6 模块，含 VITE_USE_MOCK 开关)、主布局(侧边栏+顶栏)、登录/注册页、账户/分类/交易/预算/统计五大页面(antd + recharts 图表)。
- 环境要点：本机 Bash 的 PATH 损坏(cp/ls 不可用)且 npm 触发 wsl.exe 黑名单被拦；改用 PowerShell + dangerouslyDisableSandbox=true 可正常 npm install。Bash 用 python 绝对路径可读 workspace 内文件；Read/Grep 对从桌面 docx 派生的文本文件会触发 SENSITIVE_APPROVAL，需改用 Bash+python 读取。
- 运行：依赖装完后 npm run dev；.env 默认 VITE_USE_MOCK=true，无后端也可演示。接真实后端时设 VITE_USE_MOCK=false 并启动 Spring Boot(8080)，Vite proxy 已配置转发 /api。
