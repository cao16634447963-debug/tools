# 复制为 .env 后按需修改

# 是否使用本地 Mock 数据
# true  = 不依赖后端，使用内置示例数据（适合先看界面效果）
# false = 调用真实后端接口（需同时启动 Spring Boot 后端，并通过 Vite proxy 转发 /api）
VITE_USE_MOCK=true

# 后端 API 地址（可选）
# 默认空：前端走 /api，由 vite.config.ts 的 proxy 或生产 nginx 转发到后端
# VITE_API_BASE=/api
